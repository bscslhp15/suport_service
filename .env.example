# Local defaults. Set real values in Railway Variables, never in Git.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=support_system
DB_USER=root
DB_PASSWORD=

# Optional Railway startup schema import.
DB_BOOTSTRAP=false

GEMINI_API_KEY=
SMTP_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_ENCRYPTION=tls
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_FROM_NAME=PASS College Support System
